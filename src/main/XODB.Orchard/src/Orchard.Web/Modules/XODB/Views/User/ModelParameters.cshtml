
@Html.Partial("Header")
@{
    Style.Require("XODBAdmin");

    Layout.Title = T("Update Model Parameters").ToString();
}
@if (AuthorizedFor(XODB.Permissions.ManageProjects)) {
    <h3>@Layout.Title</h3>
    Html.RenderPartial("ModelParametersPartial");
}
else {
    Html.RenderAction("UnauthorizedRedirect");
}